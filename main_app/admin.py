from django.contrib import admin
from .models import onechibi # import the Artist model from models.py
# Register your models here.

admin.site.register(onechibi) # this line will add the model to the admin panel